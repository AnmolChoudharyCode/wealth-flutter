abstract final class AppBreakpoints {
  /// Below this width: mobile layout (bottom nav)
  static const double sidebar = 600.0;

  /// Above this width: expanded sidebar with labels
  static const double wide = 1200.0;

  /// Max content width constraint
  static const double maxWidth = 1440.0;
}
